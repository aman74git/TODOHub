const navbarComp = document.querySelector('.navbar');

const registerEmailBox = document.getElementById('register-email');
const emailHelpBox = document.getElementById('emailHelp');
const registerPasswordBox = document.getElementById('register-password');
const passHelpBox = document.getElementById('passHelp');
const confirmPassBox = document.getElementById('confirmpassword');
const confirmPassHelpBox = document.getElementById('confirmPassHelp');
const registerSubmitBtn = document.getElementById('register-submit');

const loginEmailBox = document.getElementById('login-email');
const loginPasswordBox = document.getElementById('login-password');
const loginSubmitBtn = document.getElementById('login-submit');

const addTaskBox = document.getElementById('new-task-input');
const addTaskBtn = document.getElementById('add-task-btn');
const completedTaskBox = document.getElementById('completed-task-ul');
const pendingTaskBox = document.getElementById('pending-task-ul');
const noPendingComponent = document.getElementById('no-pending');
const noCompleteComponent = document.getElementById('no-complete');
